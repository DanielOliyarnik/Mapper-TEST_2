{
  "type": "object",
  "required": [
    "authoritative_semantics",
    "allowed_fields"
  ],
  "properties": {
    "authoritative_semantics": {"type": "boolean"},
    "allowed_fields": {"type": "array", "items": {"type": "string"}}
  }
}
